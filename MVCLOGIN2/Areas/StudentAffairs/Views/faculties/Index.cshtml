@model IEnumerable<MVCLOGIN2.Models.faculty>

@{
    ViewBag.Title = "Index";
}

<h2 style="text-align:center">Faculties</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-info btn-large" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.facultyname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.facultyname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.facultyID }, new { @class = "btn btn-warning btn-xs" })
            @Html.ActionLink("Details", "Details", new { id = item.facultyID }, new { @class = "btn btn-primary btn-xs" })
            @Html.ActionLink("Delete", "Delete", new { id = item.facultyID }, new { @class = "btn btn-danger btn-xs" })
        </td>
    </tr>
}

</table>
